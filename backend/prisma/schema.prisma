datasource db {
  provider = "sqlserver"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

// ------------------------------------------------------
// ENUMS
// ------------------------------------------------------

// Enums mapped to String types for MS SQL Server compatibility

// ------------------------------------------------------
// ORGANIZATIONAL HIERARCHY
// ------------------------------------------------------

model Company {
  id            String   @id @default(uuid())
  legacyMongoId String?
  code          String   @unique
  name          String
  currency      String   @default("USD")
  taxId         String?
  address       String?
  active        Boolean  @default(true)
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt

  departments      Department[]
  costCenters      CostCenter[]
  sites            Site[]
  employees        Employee[]
  assets           Asset[]
  users            User[]
  fiscalPeriods    FiscalPeriod[]
  depreciationRuns DepreciationRun[]
  receipts         Receipt[]

  @@map("companies")
}

model Department {
  id            String   @id @default(uuid())
  legacyMongoId String?
  companyId     String
  code          String
  name          String
  active        Boolean  @default(true)
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt

  company   Company    @relation(fields: [companyId], references: [id], onDelete: NoAction, onUpdate: NoAction)
  employees Employee[]
  assets    Asset[]
  users     User[]

  @@unique([companyId, code])
  @@map("departments")
}

model CostCenter {
  id            String   @id @default(uuid())
  legacyMongoId String?
  companyId     String
  code          String
  name          String
  active        Boolean  @default(true)
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt

  company   Company    @relation(fields: [companyId], references: [id], onDelete: NoAction, onUpdate: NoAction)
  employees Employee[]
  assets    Asset[]
  users     User[]

  @@unique([companyId, code])
  @@map("cost_centers")
}

model Site {
  id            String   @id @default(uuid())
  legacyMongoId String?
  companyId     String
  code          String
  name          String
  city          String?
  country       String?
  active        Boolean  @default(true)
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt

  company   Company    @relation(fields: [companyId], references: [id], onDelete: NoAction, onUpdate: NoAction)
  buildings Building[]
  assets    Asset[]
  users     User[]
  receipts  Receipt[]

  @@unique([companyId, code])
  @@map("sites")
}

model Building {
  id            String   @id @default(uuid())
  legacyMongoId String?
  siteId        String
  code          String
  name          String
  active        Boolean  @default(true)
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt

  site   Site    @relation(fields: [siteId], references: [id], onDelete: NoAction, onUpdate: NoAction)
  floors Floor[]
  assets Asset[]

  @@map("buildings")
}

model Floor {
  id            String   @id @default(uuid())
  legacyMongoId String?
  buildingId    String
  code          String
  name          String
  floorNumber   Int?
  active        Boolean  @default(true)
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt

  building Building  @relation(fields: [buildingId], references: [id], onDelete: NoAction, onUpdate: NoAction)
  rooms    Room[]
  assets   Asset[]
  floorMap FloorMap?

  @@map("floors")
}

model Room {
  id            String   @id @default(uuid())
  legacyMongoId String?
  floorId       String
  code          String
  name          String
  roomType      String   @default("General")
  active        Boolean  @default(true)
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt

  floor  Floor   @relation(fields: [floorId], references: [id], onDelete: NoAction, onUpdate: NoAction)
  zones  Zone[]
  assets Asset[]

  @@map("rooms")
}

model Zone {
  id            String   @id @default(uuid())
  legacyMongoId String?
  roomId        String
  code          String
  name          String
  active        Boolean  @default(true)
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt

  room         Room               @relation(fields: [roomId], references: [id], onDelete: NoAction, onUpdate: NoAction)
  assets       Asset[]
  mapZones     FloorMapZone[]
  mapPositions AssetMapPosition[]

  @@map("zones")
}

model Employee {
  id            String   @id @default(uuid())
  legacyMongoId String?
  employeeCode  String   @unique
  fullName      String
  email         String
  companyId     String
  departmentId  String?
  costCenterId  String?
  active        Boolean  @default(true)
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt

  company    Company     @relation(fields: [companyId], references: [id])
  department Department? @relation(fields: [departmentId], references: [id])
  costCenter CostCenter? @relation(fields: [costCenterId], references: [id])

  custodyAssets  Asset[]                @relation("AssetCustodian")
  custodyRecords CustodyAssignment[]
  users          User[]
  transfersFrom  AssetTransfer[]        @relation("TransferFromCustodian")
  transfersTo    AssetTransfer[]        @relation("TransferToCustodian")
  stocktakeObs   StocktakeObservation[]

  @@map("employees")
}

// ------------------------------------------------------
// CLASSIFICATIONS
// ------------------------------------------------------

model Category {
  id                          String             @id @default(uuid())
  legacyMongoId               String?
  code                        String             @unique
  name                        String
  description                 String?
  depreciationMethod          String             @default("STRAIGHT_LINE")
  defaultUsefulLifeMonths     Int                @default(60)
  defaultResidualValuePercent Decimal            @default(0) @db.Decimal(5, 2)
  isSerialized                Boolean            @default(true)
  active                      Boolean            @default(true)
  createdAt                   DateTime           @default(now())
  updatedAt                   DateTime           @updatedAt

  assetClasses     AssetClass[]
  models           AssetModel[]
  assets           Asset[]
  customFields     CustomFieldDefinition[]
  receiptLineItems ReceiptLineItem[]

  @@map("categories")
}

model AssetClass {
  id            String   @id @default(uuid())
  legacyMongoId String?
  code          String   @unique
  name          String
  categoryId    String
  description   String?
  active        Boolean  @default(true)
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt

  category Category @relation(fields: [categoryId], references: [id])
  assets   Asset[]

  @@map("asset_classes")
}

model Manufacturer {
  id             String   @id @default(uuid())
  legacyMongoId  String?
  name           String   @unique
  supportContact String?
  website        String?
  active         Boolean  @default(true)
  createdAt      DateTime @default(now())
  updatedAt      DateTime @updatedAt

  models AssetModel[]
  assets Asset[]

  @@map("manufacturers")
}

model AssetModel {
  id             String   @id @default(uuid())
  legacyMongoId  String?
  modelNumber    String
  name           String
  manufacturerId String
  categoryId     String
  specifications String?
  active         Boolean  @default(true)
  createdAt      DateTime @default(now())
  updatedAt      DateTime @updatedAt

  manufacturer Manufacturer @relation(fields: [manufacturerId], references: [id])
  category     Category     @relation(fields: [categoryId], references: [id])
  assets       Asset[]

  @@map("asset_models")
}

// ------------------------------------------------------
// CORE ASSET
// ------------------------------------------------------

model Asset {
  id            String  @id @default(uuid())
  legacyMongoId String?
  assetId       String  @unique
  legacyId      String?
  tagNumber     String? @unique
  barcode       String?
  qrCode        String?
  rfidEpc       String?
  rfidTid       String?
  serialNumber  String?
  description   String

  categoryId     String
  assetClassId   String?
  manufacturerId String?
  modelId        String?
  parentAssetId  String?

  lifecycleStatus String @default("RECEIVED")
  condition       String @default("NEW")
  criticality     String @default("MEDIUM")

  companyId    String
  siteId       String
  buildingId   String?
  floorId      String?
  roomId       String?
  zoneId       String?
  departmentId String?
  costCenterId String?

  custodianId  String?
  assignedDate DateTime?

  poNumber         String?
  supplierName     String?
  purchaseDate     DateTime?
  inServiceDate    DateTime?
  acquisitionValue Decimal   @default(0) @db.Decimal(18, 2)
  currency         String    @default("USD")

  hostname    String?
  macAddress  String?
  ipAddress   String?
  discoveryId String?

  healthScore Int @default(100)

  createdByUserId String?
  updatedByUserId String?
  active          Boolean  @default(true)
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt

  // Relations
  category     Category      @relation(fields: [categoryId], references: [id], onDelete: NoAction, onUpdate: NoAction)
  assetClass   AssetClass?   @relation(fields: [assetClassId], references: [id], onDelete: NoAction, onUpdate: NoAction)
  manufacturer Manufacturer? @relation(fields: [manufacturerId], references: [id], onDelete: NoAction, onUpdate: NoAction)
  model        AssetModel?   @relation(fields: [modelId], references: [id], onDelete: NoAction, onUpdate: NoAction)
  parentAsset  Asset?        @relation("ChildAssets", fields: [parentAssetId], references: [id], onDelete: NoAction, onUpdate: NoAction)
  childAssets  Asset[]       @relation("ChildAssets")

  company    Company     @relation(fields: [companyId], references: [id], onDelete: NoAction, onUpdate: NoAction)
  site       Site        @relation(fields: [siteId], references: [id], onDelete: NoAction, onUpdate: NoAction)
  building   Building?   @relation(fields: [buildingId], references: [id], onDelete: NoAction, onUpdate: NoAction)
  floor      Floor?      @relation(fields: [floorId], references: [id], onDelete: NoAction, onUpdate: NoAction)
  room       Room?       @relation(fields: [roomId], references: [id], onDelete: NoAction, onUpdate: NoAction)
  zone       Zone?       @relation(fields: [zoneId], references: [id], onDelete: NoAction, onUpdate: NoAction)
  department Department? @relation(fields: [departmentId], references: [id], onDelete: NoAction, onUpdate: NoAction)
  costCenter CostCenter? @relation(fields: [costCenterId], references: [id], onDelete: NoAction, onUpdate: NoAction)
  custodian  Employee?   @relation("AssetCustodian", fields: [custodianId], references: [id], onDelete: NoAction, onUpdate: NoAction)

  creator User? @relation("AssetCreator", fields: [createdByUserId], references: [id], onDelete: NoAction, onUpdate: NoAction)
  updater User? @relation("AssetUpdater", fields: [updatedByUserId], references: [id], onDelete: NoAction, onUpdate: NoAction)

  // Dependent Relational Lists
  customFieldValues  AssetCustomFieldValue[]
  bookValues         AssetBookValue[]
  custodyAssignments CustodyAssignment[]
  transfers          AssetTransfer[]
  workOrders         MaintenanceWorkOrder[]
  schedules          MaintenanceSchedule[]
  contractLinks      ContractAsset[]
  warranty           Warranty?
  tags               Tag[]
  tagHistories       TagHistory[]
  mapPosition        AssetMapPosition?
  discoveryMatches   DiscoveryMatch[]
  aiRecommendations  AIRecommendation[]
  transactions       AssetTransaction[]
  rtlsLocation       RtlsAssetLocation?
  rtlsMovements      RtlsMovement[]

  @@index([companyId, siteId, categoryId, lifecycleStatus])
  @@index([assetId, serialNumber, tagNumber, barcode, hostname])
  @@map("assets")
}

model AssetCustomFieldValue {
  id               String    @id @default(uuid())
  assetId          String
  customFieldDefId String
  textValue        String?
  numberValue      Decimal?  @db.Decimal(18, 4)
  dateValue        DateTime?
  booleanValue     Boolean?
  createdAt        DateTime  @default(now())
  updatedAt        DateTime  @updatedAt

  asset          Asset                 @relation(fields: [assetId], references: [id], onDelete: NoAction, onUpdate: NoAction)
  customFieldDef CustomFieldDefinition @relation(fields: [customFieldDefId], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@unique([assetId, customFieldDefId])
  @@map("asset_custom_field_values")
}

model CustomFieldDefinition {
  id            String    @id @default(uuid())
  legacyMongoId String?
  name          String    @unique
  label         String
  fieldType     String
  required      Boolean   @default(false)
  defaultValue  String?
  options       String?
  categoryId    String?
  active        Boolean   @default(true)
  createdAt     DateTime  @default(now())
  updatedAt     DateTime  @updatedAt

  category Category?               @relation(fields: [categoryId], references: [id])
  values   AssetCustomFieldValue[]

  @@map("custom_field_definitions")
}

model AssetTransaction {
  id                String   @id @default(uuid())
  assetId           String
  transactionType   String
  fromStatus        String?
  toStatus          String?
  performedByUserId String
  timestamp         DateTime @default(now())
  notes             String?
  payload           String?
  createdAt         DateTime @default(now())
  updatedAt         DateTime @updatedAt

  asset       Asset @relation(fields: [assetId], references: [id], onDelete: NoAction, onUpdate: NoAction)
  performedBy User  @relation(fields: [performedByUserId], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([assetId, timestamp])
  @@map("asset_transactions")
}

// ------------------------------------------------------
// USER, SECURITY & AUTHORIZATION (RBAC + DATA SCOPE)
// ------------------------------------------------------

model User {
  id            String    @id @default(uuid())
  legacyMongoId String?
  username      String    @unique
  email         String    @unique
  passwordHash  String
  fullName      String
  phone         String?
  roleId        String
  companyId     String?
  siteId        String?
  departmentId  String?
  costCenterId  String?
  employeeId    String?
  active        Boolean   @default(true)
  lastLogin     DateTime?
  createdAt     DateTime  @default(now())
  updatedAt     DateTime  @updatedAt

  role       Role        @relation(fields: [roleId], references: [id], onDelete: NoAction, onUpdate: NoAction)
  company    Company?    @relation(fields: [companyId], references: [id], onDelete: NoAction, onUpdate: NoAction)
  site       Site?       @relation(fields: [siteId], references: [id], onDelete: NoAction, onUpdate: NoAction)
  department Department? @relation(fields: [departmentId], references: [id], onDelete: NoAction, onUpdate: NoAction)
  costCenter CostCenter? @relation(fields: [costCenterId], references: [id], onDelete: NoAction, onUpdate: NoAction)
  employee   Employee?   @relation(fields: [employeeId], references: [id], onDelete: NoAction, onUpdate: NoAction)

  dataScopes          UserDataScope[]
  createdAssets       Asset[]                @relation("AssetCreator")
  updatedAssets       Asset[]                @relation("AssetUpdater")
  issuedCustodies     CustodyAssignment[]    @relation("CustodyIssuer")
  requestedTransfers  AssetTransfer[]        @relation("TransferRequester")
  approvedTransfers   AssetTransfer[]        @relation("TransferApprover")
  receivedTransfers   AssetTransfer[]        @relation("TransferReceiver")
  workOrdersAssigned  MaintenanceWorkOrder[] @relation("TechnicianWorkOrders")
  workOrdersCreated   MaintenanceWorkOrder[] @relation("CreatedWorkOrders")
  auditEvents         AuditEvent[]
  notifications       Notification[]
  postedDepreciations DepreciationRun[]
  importJobs          ImportJob[]
  assetTransactions   AssetTransaction[]

  @@map("users")
}

model Role {
  id            String   @id @default(uuid())
  legacyMongoId String?
  name          String   @unique
  code          String   @unique
  description   String?
  permissions   String?
  isSystem      Boolean  @default(false)
  active        Boolean  @default(true)
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt

  users User[]

  @@map("roles")
}

model DataScope {
  id            String   @id @default(uuid())
  legacyMongoId String?
  name          String
  companyIds    String?
  siteIds       String?
  departmentIds String?
  costCenterIds String?
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt

  userAssignments UserDataScope[]

  @@map("data_scopes")
}

model UserDataScope {
  userId      String
  dataScopeId String
  user        User      @relation(fields: [userId], references: [id], onDelete: Cascade)
  dataScope   DataScope @relation(fields: [dataScopeId], references: [id], onDelete: Cascade)

  @@id([userId, dataScopeId])
  @@map("user_data_scopes")
}

model AuditEvent {
  id          String   @id @default(uuid())
  userId      String?
  action      String
  entityType  String
  entityId    String?
  beforeState String?
  afterState  String?
  ipAddress   String?
  userAgent   String?
  timestamp   DateTime @default(now())
  createdAt   DateTime @default(now())

  user User? @relation(fields: [userId], references: [id])

  @@index([userId, entityType, timestamp])
  @@map("audit_events")
}

model Notification {
  id        String           @id @default(uuid())
  userId    String
  title     String
  message   String
  type      String           @default("INFO")
  read      Boolean          @default(false)
  link      String?
  createdAt DateTime         @default(now())
  updatedAt DateTime         @updatedAt

  user User @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@map("notifications")
}

// ------------------------------------------------------
// CUSTODY & TRANSFERS
// ------------------------------------------------------

model CustodyAssignment {
  id                  String    @id @default(uuid())
  legacyMongoId       String?
  assetId             String
  custodianId         String
  issuedDate          DateTime  @default(now())
  expectedReturnDate  DateTime?
  actualReturnDate    DateTime?
  conditionAtIssue    String?
  conditionAtReturn   String?
  issuedByUserId      String
  acknowledged        Boolean   @default(false)
  acknowledgementDate DateTime?
  active              Boolean   @default(true)
  createdAt           DateTime  @default(now())
  updatedAt           DateTime  @updatedAt

  asset     Asset    @relation(fields: [assetId], references: [id], onDelete: NoAction, onUpdate: NoAction)
  custodian Employee @relation(fields: [custodianId], references: [id], onDelete: NoAction, onUpdate: NoAction)
  issuedBy  User     @relation("CustodyIssuer", fields: [issuedByUserId], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@map("custody_assignments")
}

model AssetTransfer {
  id             String       @id @default(uuid())
  legacyMongoId  String?
  transferNumber String       @unique
  assetId        String
  transferType   String

  fromCompanyId   String?
  fromSiteId      String?
  fromRoomId      String?
  fromCustodianId String?

  toCompanyId   String?
  toSiteId      String?
  toRoomId      String?
  toCustodianId String?

  status            String @default("DRAFT")
  reason            String?
  requestedByUserId String
  approvedByUserId  String?
  receivedByUserId  String?
  dispatchDate      DateTime?
  receiveDate       DateTime?
  createdAt         DateTime       @default(now())
  updatedAt         DateTime       @updatedAt

  asset         Asset     @relation(fields: [assetId], references: [id], onDelete: NoAction, onUpdate: NoAction)
  fromCustodian Employee? @relation("TransferFromCustodian", fields: [fromCustodianId], references: [id], onDelete: NoAction, onUpdate: NoAction)
  toCustodian   Employee? @relation("TransferToCustodian", fields: [toCustodianId], references: [id], onDelete: NoAction, onUpdate: NoAction)
  requestedBy   User      @relation("TransferRequester", fields: [requestedByUserId], references: [id], onDelete: NoAction, onUpdate: NoAction)
  approvedBy    User?     @relation("TransferApprover", fields: [approvedByUserId], references: [id], onDelete: NoAction, onUpdate: NoAction)
  receivedBy    User?     @relation("TransferReceiver", fields: [receivedByUserId], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@map("asset_transfers")
}

// ------------------------------------------------------
// FINANCIAL & DEPRECIATION
// ------------------------------------------------------

model AssetBookValue {
  id                      String             @id @default(uuid())
  legacyMongoId           String?
  assetId                 String
  bookType                String             @default("CORPORATE")
  capitalizationDate      DateTime?
  capitalizationValue     Decimal            @db.Decimal(18, 2)
  usefulLifeMonths        Int
  depreciationMethod      String             @default("STRAIGHT_LINE")
  residualValue           Decimal            @default(0) @db.Decimal(18, 2)
  accumulatedDepreciation Decimal            @default(0) @db.Decimal(18, 2)
  netBookValue            Decimal            @db.Decimal(18, 2)
  isLocked                Boolean            @default(false)
  createdAt               DateTime           @default(now())
  updatedAt               DateTime           @updatedAt

  asset Asset @relation(fields: [assetId], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@unique([assetId, bookType])
  @@map("asset_book_values")
}

model FiscalPeriod {
  id             String    @id @default(uuid())
  legacyMongoId  String?
  companyId      String
  year           Int
  periodNumber   Int
  periodName     String?
  startDate      DateTime
  endDate        DateTime
  isClosed       Boolean   @default(false)
  closedAt       DateTime?
  closedByUserId String?
  createdAt      DateTime  @default(now())
  updatedAt      DateTime  @updatedAt

  company             Company             @relation(fields: [companyId], references: [id])
  depreciationRuns    DepreciationRun[]
  depreciationEntries DepreciationEntry[]

  @@unique([companyId, year, periodNumber])
  @@map("fiscal_periods")
}

model DepreciationRun {
  id                      String                @id @default(uuid())
  legacyMongoId           String?
  runNumber               String                @unique
  companyId               String
  fiscalPeriodId          String
  bookType                String                @default("CORPORATE")
  status                  String                @default("DRAFT")
  totalAssetsProcessed    Int                   @default(0)
  totalDepreciationAmount Decimal               @default(0) @db.Decimal(18, 2)
  postedAt                DateTime?
  postedByUserId          String?
  createdAt               DateTime              @default(now())
  updatedAt               DateTime              @updatedAt

  company      Company             @relation(fields: [companyId], references: [id], onDelete: NoAction, onUpdate: NoAction)
  fiscalPeriod FiscalPeriod        @relation(fields: [fiscalPeriodId], references: [id], onDelete: NoAction, onUpdate: NoAction)
  postedBy     User?               @relation(fields: [postedByUserId], references: [id], onDelete: NoAction, onUpdate: NoAction)
  entries      DepreciationEntry[]

  @@map("depreciation_runs")
}

model DepreciationEntry {
  id                      String   @id @default(uuid())
  legacyMongoId           String?
  depreciationRunId       String
  assetId                 String
  fiscalPeriodId          String
  openingNetBookValue     Decimal  @db.Decimal(18, 2)
  depreciationAmount      Decimal  @db.Decimal(18, 2)
  accumulatedDepreciation Decimal  @db.Decimal(18, 2)
  closingNetBookValue     Decimal  @db.Decimal(18, 2)
  isPosted                Boolean  @default(false)
  createdAt               DateTime @default(now())
  updatedAt               DateTime @updatedAt

  depreciationRun DepreciationRun @relation(fields: [depreciationRunId], references: [id], onDelete: NoAction, onUpdate: NoAction)
  fiscalPeriod    FiscalPeriod    @relation(fields: [fiscalPeriodId], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@unique([depreciationRunId, assetId])
  @@map("depreciation_entries")
}

// ------------------------------------------------------
// MAINTENANCE & WORK ORDERS
// ------------------------------------------------------

model MaintenanceSchedule {
  id                String    @id @default(uuid())
  legacyMongoId     String?
  title             String
  assetId           String
  frequencyMonths   Int       @default(6)
  lastPerformedDate DateTime?
  nextDueDate       DateTime
  active            Boolean   @default(true)
  createdAt         DateTime  @default(now())
  updatedAt         DateTime  @updatedAt

  asset Asset @relation(fields: [assetId], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@map("maintenance_schedules")
}

model MaintenanceWorkOrder {
  id              String           @id @default(uuid())
  legacyMongoId   String?
  workOrderNumber String           @unique
  assetId         String
  workType        String           @default("CORRECTIVE")
  priority        String           @default("MEDIUM")
  status          String           @default("OPEN")

  description String
  failureCode String?
  rootCause   String?

  assignedTechnicianId String?
  vendorId             String?
  vendorName           String?

  scheduledDate        DateTime?
  startedDate          DateTime?
  completedDate        DateTime?
  completionTargetDate DateTime?

  laborHours   Decimal @default(0) @db.Decimal(10, 2)
  laborCost    Decimal @default(0) @db.Decimal(18, 2)
  partsCost    Decimal @default(0) @db.Decimal(18, 2)
  cost         Decimal @default(0) @db.Decimal(18, 2)
  meterReading Int?

  notes           String?
  photoUrls       String?
  createdByUserId String
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt

  asset              Asset @relation(fields: [assetId], references: [id], onDelete: NoAction, onUpdate: NoAction)
  assignedTechnician User? @relation("TechnicianWorkOrders", fields: [assignedTechnicianId], references: [id], onDelete: NoAction, onUpdate: NoAction)
  createdBy          User  @relation("CreatedWorkOrders", fields: [createdByUserId], references: [id], onDelete: NoAction, onUpdate: NoAction)

  partsUsed WorkOrderPart[]
  checklist WorkOrderChecklist[]

  @@map("maintenance_work_orders")
}

model WorkOrderPart {
  id          String  @id @default(uuid())
  workOrderId String
  partName    String
  quantity    Int     @default(1)
  unitCost    Decimal @default(0) @db.Decimal(18, 2)
  totalCost   Decimal @default(0) @db.Decimal(18, 2)

  workOrder MaintenanceWorkOrder @relation(fields: [workOrderId], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@map("work_order_parts")
}

model WorkOrderChecklist {
  id          String  @id @default(uuid())
  workOrderId String
  task        String
  completed   Boolean @default(false)
  notes       String?

  workOrder MaintenanceWorkOrder @relation(fields: [workOrderId], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@map("work_order_checklists")
}

// ------------------------------------------------------
// CONTRACTS, WARRANTIES & INTAKE RECEIPTS
// ------------------------------------------------------

model Contract {
  id             String       @id @default(uuid())
  legacyMongoId  String?
  contractNumber String       @unique
  title          String
  contractType   String
  providerName   String
  startDate      DateTime
  endDate        DateTime
  cost           Decimal      @default(0) @db.Decimal(18, 2)
  slaDetails     String?
  active         Boolean      @default(true)
  createdAt      DateTime     @default(now())
  updatedAt      DateTime     @updatedAt

  coveredAssets ContractAsset[]

  @@map("contracts")
}

model ContractAsset {
  contractId String
  assetId    String
  contract   Contract @relation(fields: [contractId], references: [id], onDelete: NoAction, onUpdate: NoAction)
  asset      Asset    @relation(fields: [assetId], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@id([contractId, assetId])
  @@map("contract_assets")
}

model Warranty {
  id             String               @id @default(uuid())
  legacyMongoId  String?
  assetId        String               @unique
  providerName   String?
  warrantyNumber String?
  startDate      DateTime
  endDate        DateTime
  terms          String?
  coverageType   String               @default("FULL")
  createdAt      DateTime             @default(now())
  updatedAt      DateTime             @updatedAt

  asset Asset @relation(fields: [assetId], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@map("warranties")
}

model Receipt {
  id               String   @id @default(uuid())
  legacyMongoId    String?
  receiptNumber    String   @unique
  poNumber         String
  vendorName       String
  companyId        String
  siteId           String
  receivedDate     DateTime @default(now())
  status           String   @default("STAGED")
  receivedByUserId String
  createdAt        DateTime @default(now())
  updatedAt        DateTime @updatedAt

  company   Company           @relation(fields: [companyId], references: [id], onDelete: NoAction, onUpdate: NoAction)
  site      Site              @relation(fields: [siteId], references: [id], onDelete: NoAction, onUpdate: NoAction)
  lineItems ReceiptLineItem[]

  @@map("receipts")
}

model ReceiptLineItem {
  id              String   @id @default(uuid())
  receiptId       String
  description     String?
  quantity        Int?
  unitPrice       Decimal? @db.Decimal(18, 2)
  categoryId      String?
  serialNumbers   String?
  createdAssetIds String?

  receipt  Receipt   @relation(fields: [receiptId], references: [id], onDelete: NoAction, onUpdate: NoAction)
  category Category? @relation(fields: [categoryId], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@map("receipt_line_items")
}

// ------------------------------------------------------
// STOCKTAKE CAMPAIGNS
// ------------------------------------------------------

model StocktakeCampaign {
  id              String          @id @default(uuid())
  legacyMongoId   String?
  campaignNumber  String          @unique
  title           String
  companyId       String
  siteId          String
  buildingId      String?
  floorId         String?
  categoryId      String?
  mode            String          @default("FULL_CENSUS")
  status          String          @default("DRAFT")
  startDate       DateTime?
  endDate         DateTime?
  createdByUserId String

  totalExpected     Int @default(0)
  totalVerified     Int @default(0)
  totalRelocated    Int @default(0)
  totalMissing      Int @default(0)
  totalUnregistered Int @default(0)

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  expectedAssets StocktakeExpectedAsset[]
  observations   StocktakeObservation[]
  exceptions     StocktakeException[]

  @@map("stocktake_campaigns")
}

model StocktakeExpectedAsset {
  id                  String               @id @default(uuid())
  campaignId          String
  assetId             String
  expectedSiteId      String?
  expectedRoomId      String?
  expectedCustodianId String?
  status              String               @default("PENDING")
  createdAt           DateTime             @default(now())
  updatedAt           DateTime             @updatedAt

  campaign StocktakeCampaign @relation(fields: [campaignId], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@unique([campaignId, assetId])
  @@map("stocktake_expected_assets")
}

model StocktakeObservation {
  id                     String    @id @default(uuid())
  campaignId             String
  assetId                String?
  scannedTagNumber       String?
  scannedSerial          String?
  scanType               String    @default("BARCODE")
  observedRoomId         String?
  observedCustodianId    String?
  observedCondition      String?
  observedByUserId       String
  timestamp              DateTime  @default(now())
  deviceTimestamp        DateTime?
  offlineTransactionUUID String?
  photoUrl               String?
  createdAt              DateTime  @default(now())
  updatedAt              DateTime  @updatedAt

  campaign          StocktakeCampaign @relation(fields: [campaignId], references: [id], onDelete: NoAction, onUpdate: NoAction)
  observedCustodian Employee?         @relation(fields: [observedCustodianId], references: [id])

  @@map("stocktake_observations")
}

model StocktakeException {
  id               String           @id @default(uuid())
  campaignId       String
  assetId          String?
  exceptionType    String
  details          String?
  resolutionStatus String           @default("OPEN")
  resolutionNotes  String?
  resolvedByUserId String?
  resolvedAt       DateTime?
  createdAt        DateTime         @default(now())
  updatedAt        DateTime         @updatedAt

  campaign StocktakeCampaign @relation(fields: [campaignId], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@map("stocktake_exceptions")
}

// ------------------------------------------------------
// TAGGING, DISCOVERY & SPATIAL MAPS
// ------------------------------------------------------

model Tag {
  id            String    @id @default(uuid())
  legacyMongoId String?
  tagNumber     String    @unique
  tagType       String    @default("BARCODE_128")
  rfidEpc       String?   @unique
  rfidTid       String?   @unique
  assetId       String?
  status        String    @default("UNASSIGNED")
  printedDate   DateTime?
  createdAt     DateTime  @default(now())
  updatedAt     DateTime  @updatedAt

  asset Asset? @relation(fields: [assetId], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@map("tags")
}

model TagHistory {
  id               String   @id @default(uuid())
  assetId          String
  oldTagNumber     String?
  newTagNumber     String
  reason           String
  replacedByUserId String
  timestamp        DateTime @default(now())
  createdAt        DateTime @default(now())

  asset Asset @relation(fields: [assetId], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@map("tag_histories")
}

model DiscoveryObservation {
  id                  String          @id @default(uuid())
  legacyMongoId       String?
  discoverySource     String          @default("IP_SCANNER")
  ipAddress           String?
  macAddress          String?
  hostname            String?
  serialNumber        String?
  manufacturer        String?
  modelName           String?
  osFamily            String?
  osVersion           String?
  cpuInfo             String?
  ramGb               Int?
  storageGb           Int?
  loggedOnUser        String?
  firstSeen           DateTime        @default(now())
  lastSeen            DateTime        @default(now())
  rawCollectorPayload String?
  createdAt           DateTime        @default(now())
  updatedAt           DateTime        @updatedAt

  matches DiscoveryMatch[]

  @@map("discovery_observations")
}

model DiscoveryMatch {
  id               String      @id @default(uuid())
  observationId    String
  matchedAssetId   String?
  confidenceScore  Int
  matchRule        String
  status           String      @default("SUGGESTED")
  reviewedByUserId String?
  reviewedAt       DateTime?
  createdAt        DateTime    @default(now())
  updatedAt        DateTime    @updatedAt

  observation  DiscoveryObservation @relation(fields: [observationId], references: [id], onDelete: NoAction, onUpdate: NoAction)
  matchedAsset Asset?               @relation(fields: [matchedAssetId], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@map("discovery_matches")
}

model FloorMap {
  id            String   @id @default(uuid())
  legacyMongoId String?
  title         String
  floorId       String   @unique
  imageUrl      String
  widthMeters   Decimal  @default(50) @db.Decimal(10, 2)
  heightMeters  Decimal  @default(30) @db.Decimal(10, 2)
  active        Boolean  @default(true)
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt

  floor          Floor              @relation(fields: [floorId], references: [id], onDelete: NoAction, onUpdate: NoAction)
  zones          FloorMapZone[]
  assetPositions AssetMapPosition[]

  @@map("floor_maps")
}

model FloorMapZone {
  id         String  @id @default(uuid())
  floorMapId String
  zoneId     String?
  name       String
  color      String  @default("#3B82F6")
  points     String // JSON String array of {x, y} coordinates

  floorMap FloorMap @relation(fields: [floorMapId], references: [id], onDelete: NoAction, onUpdate: NoAction)
  zone     Zone?    @relation(fields: [zoneId], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@map("floor_map_zones")
}

model AssetMapPosition {
  id              String   @id @default(uuid())
  legacyMongoId   String?
  assetId         String   @unique
  floorMapId      String
  xRatio          Decimal  @db.Decimal(6, 4)
  yRatio          Decimal  @db.Decimal(6, 4)
  zoneId          String?
  updatedByUserId String
  active          Boolean  @default(true)
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt

  asset    Asset    @relation(fields: [assetId], references: [id], onDelete: NoAction, onUpdate: NoAction)
  floorMap FloorMap @relation(fields: [floorMapId], references: [id], onDelete: NoAction, onUpdate: NoAction)
  zone     Zone?    @relation(fields: [zoneId], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@map("asset_map_positions")
}

// ------------------------------------------------------
// WORKFLOWS, AI INSIGHTS & ATTACHMENTS
// ------------------------------------------------------

model WorkflowDefinition {
  id              String                  @id @default(uuid())
  legacyMongoId   String?
  name            String
  transactionType String                  @unique
  valueThreshold  Decimal                 @default(0) @db.Decimal(18, 2)
  active          Boolean                 @default(true)
  createdAt       DateTime                @default(now())
  updatedAt       DateTime                @updatedAt

  steps     WorkflowStep[]
  instances WorkflowInstance[]

  @@map("workflow_definitions")
}

model WorkflowStep {
  id                   String  @id @default(uuid())
  workflowDefinitionId String
  stepNumber           Int
  name                 String
  approverRoleCode     String
  isParallel           Boolean @default(false)
  slaHours             Int     @default(48)

  workflowDefinition WorkflowDefinition @relation(fields: [workflowDefinitionId], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@map("workflow_steps")
}

model WorkflowInstance {
  id                   String                 @id @default(uuid())
  legacyMongoId        String?
  workflowDefinitionId String
  entityType           String
  entityId             String
  currentStepNumber    Int                    @default(1)
  status               String                 @default("PENDING")
  requestedByUserId    String
  createdAt            DateTime               @default(now())
  updatedAt            DateTime               @updatedAt

  workflowDefinition WorkflowDefinition @relation(fields: [workflowDefinitionId], references: [id])
  approvalActions    ApprovalAction[]

  @@map("workflow_instances")
}

model ApprovalAction {
  id                 String           @id @default(uuid())
  legacyMongoId      String?
  workflowInstanceId String
  stepNumber         Int
  approverUserId     String
  decision           String
  comments           String?
  timestamp          DateTime         @default(now())
  createdAt          DateTime         @default(now())

  workflowInstance WorkflowInstance @relation(fields: [workflowInstanceId], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@map("approval_actions")
}

model AIRecommendation {
  id                    String                 @id @default(uuid())
  legacyMongoId         String?
  type                  String
  assetId               String?
  confidenceScore       Int
  recommendationPayload String?
  explanation           String?
  status                String                 @default("PENDING")
  reviewedByUserId      String?
  reviewedAt            DateTime?
  createdAt             DateTime               @default(now())
  updatedAt             DateTime               @updatedAt

  asset Asset? @relation(fields: [assetId], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@map("ai_recommendations")
}

model Attachment {
  id               String   @id @default(uuid())
  legacyMongoId    String?
  entityType       String
  entityId         String
  fileName         String
  fileType         String?
  fileSize         Int?
  storageKey       String
  url              String
  uploadedByUserId String?
  createdAt        DateTime @default(now())
  updatedAt        DateTime @updatedAt

  @@index([entityType, entityId])
  @@map("attachments")
}

// ------------------------------------------------------
// INTEGRATIONS & SYSTEM JOBS
// ------------------------------------------------------

model WebhookSubscription {
  id            String   @id @default(uuid())
  legacyMongoId String?
  targetUrl     String
  events        String?
  secret        String?
  active        Boolean  @default(true)
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt

  deliveries WebhookDelivery[]

  @@map("webhook_subscriptions")
}

model WebhookDelivery {
  id             String   @id @default(uuid())
  subscriptionId String
  event          String?
  payload        String?
  responseStatus Int?
  success        Boolean?
  timestamp      DateTime @default(now())
  createdAt      DateTime @default(now())

  subscription WebhookSubscription @relation(fields: [subscriptionId], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@map("webhook_deliveries")
}

model ImportJob {
  id              String          @id @default(uuid())
  legacyMongoId   String?
  fileName        String
  entityType      String
  totalRows       Int             @default(0)
  processedRows   Int             @default(0)
  successRows     Int             @default(0)
  failedRows      Int             @default(0)
  errors          String? // JSON String array of {row, error}
  status          String          @default("PENDING")
  createdByUserId String
  createdAt       DateTime        @default(now())
  updatedAt       DateTime        @updatedAt

  createdBy User @relation(fields: [createdByUserId], references: [id])

  @@map("import_jobs")
}

// ------------------------------------------------------
// REAL-TIME LOCATION TRACKING (RTLS) & RFID READERS
// ------------------------------------------------------

// RTLS Enums mapped to String types for MS SQL Server compatibility

model RtlsReader {
  id               String           @id @default(uuid())
  readerIdentifier String           @unique
  name             String
  manufacturer     String           @default("Impinj")
  modelName        String?          @default("Speedway R420")
  ipAddress        String?
  macAddress       String?
  siteId           String
  buildingId       String?
  floorId          String?
  zoneId           String?
  status           String           @default("ONLINE")
  lastHeartbeat    DateTime         @default(now())
  firmwareVersion  String?          @default("v5.14.0")
  enabled          Boolean          @default(true)
  createdAt        DateTime         @default(now())
  updatedAt        DateTime         @updatedAt

  antennas   RtlsAntenna[]
  events     RfidEvent[]
  locations  RtlsAssetLocation[]
  movements  RtlsMovement[]
  heartbeats RtlsReaderHeartbeat[]
  alerts     RtlsAlert[]

  @@map("rtls_readers")
}

model RtlsAntenna {
  id            String   @id @default(uuid())
  readerId      String
  antennaNumber Int
  name          String
  zoneId        String?
  direction     String?  @default("OMNIDIRECTIONAL")
  enabled       Boolean  @default(true)
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt

  reader    RtlsReader          @relation(fields: [readerId], references: [id], onDelete: NoAction, onUpdate: NoAction)
  events    RfidEvent[]
  locations RtlsAssetLocation[]
  movements RtlsMovement[]

  @@unique([readerId, antennaNumber])
  @@map("rtls_antennas")
}

model RfidEvent {
  id               String   @id @default(uuid())
  epc              String
  tid              String?
  readerId         String
  antennaId        String?
  rssi             Int?     @default(-55)
  rawPayload       String?
  timestamp        DateTime @default(now())
  processedAt      DateTime @default(now())
  processingStatus String   @default("PROCESSED")

  reader  RtlsReader   @relation(fields: [readerId], references: [id], onDelete: NoAction, onUpdate: NoAction)
  antenna RtlsAntenna? @relation(fields: [antennaId], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([epc, timestamp])
  @@index([readerId, timestamp])
  @@map("rfid_events")
}

model RtlsAssetLocation {
  id         String   @id @default(uuid())
  assetId    String   @unique
  zoneId     String?
  readerId   String?
  antennaId  String?
  confidence Int      @default(90)
  firstSeen  DateTime @default(now())
  lastSeen   DateTime @default(now())
  source     String   @default("RFID_RTLS")
  xRatio     Decimal? @db.Decimal(6, 4)
  yRatio     Decimal? @db.Decimal(6, 4)
  createdAt  DateTime @default(now())
  updatedAt  DateTime @updatedAt

  asset   Asset        @relation(fields: [assetId], references: [id], onDelete: NoAction, onUpdate: NoAction)
  reader  RtlsReader?  @relation(fields: [readerId], references: [id], onDelete: NoAction, onUpdate: NoAction)
  antenna RtlsAntenna? @relation(fields: [antennaId], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@map("rtls_asset_locations")
}

model RtlsMovement {
  id         String   @id @default(uuid())
  assetId    String
  fromZoneId String?
  toZoneId   String?
  readerId   String?
  antennaId  String?
  detectedAt DateTime @default(now())
  direction  String?  @default("TRANSIT")
  confidence Int      @default(90)
  eventId    String?
  createdAt  DateTime @default(now())

  asset   Asset        @relation(fields: [assetId], references: [id], onDelete: NoAction, onUpdate: NoAction)
  reader  RtlsReader?  @relation(fields: [readerId], references: [id], onDelete: NoAction, onUpdate: NoAction)
  antenna RtlsAntenna? @relation(fields: [antennaId], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([assetId, detectedAt])
  @@map("rtls_movements")
}

model RtlsReaderHeartbeat {
  id        String   @id @default(uuid())
  readerId  String
  timestamp DateTime @default(now())
  status    String   @default("ONLINE")
  latencyMs Int      @default(12)
  errorInfo String?
  createdAt DateTime @default(now())

  reader RtlsReader @relation(fields: [readerId], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([readerId, timestamp])
  @@map("rtls_reader_heartbeats")
}

model RtlsAlert {
  id               String            @id @default(uuid())
  alertType        String
  severity         String            @default("HIGH")
  message          String
  assetId          String?
  readerId         String?
  zoneId           String?
  status           String            @default("OPEN")
  createdAt        DateTime          @default(now())
  resolvedAt       DateTime?
  resolvedByUserId String?

  reader RtlsReader? @relation(fields: [readerId], references: [id])

  @@index([status, createdAt])
  @@map("rtls_alerts")
}
