import React from 'react';
import { DiscoveredDevicesWorkbench } from './DiscoveredDevicesWorkbench';

export function DiscoveredDevices(props) {
  return <DiscoveredDevicesWorkbench {...props} />;
}

export default DiscoveredDevices;
